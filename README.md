Standalone Giveaway Webapp (Node.js + Express + SQLite + EJS + Tailwind)

Quick start:

1. npm install
2. copy .env (already included) or edit values
3. npm run seed
4. npm start
5. Visit http://localhost:3000 and /admin/login

Default admin credentials are set in .env
